<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.smhrd.project.campusOracleMapper.campusOracleMapper">

	<insert id="createUser" parameterType="java.util.Map">
		BEGIN
		EXECUTE IMMEDIATE 'CREATE USER ${username} IDENTIFIED BY ${password}';
		END;
	</insert>

	<update id="grantPrivileges">
		BEGIN
		EXECUTE IMMEDIATE 'GRANT CONNECT, RESOURCE, CREATE VIEW, CREATE SESSION TO ${username}';
		END;
	</update>

</mapper>